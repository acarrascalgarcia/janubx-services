frameworkVersion: ">=2.0.0 <=2.30.0"

package:
  exclude:
    - local/**
    - node_modules/**
    - .idea/**
    - .requirements/**
    - env/**
    - README.md
    - package.json
    - package-lock.json
    - requirements.txt

provider:
  name: aws
  runtime: python3.8
  region: us-east-1
  stage: dev
